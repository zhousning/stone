.container-fluid
  = form_for [@designer_electric, @designer_electric_cert], :builder => WebsiteFormBuilder, :html => {:'data-parsley-validate' => true} do |f|
    - if @designer_electric_cert.errors.any?
      #error_explanation.alert.alert-danger.alert-dismissible.fade.show
        %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
          %span{"aria-hidden" => "true"} x 
        %h2= "#{pluralize(@designer_electric_cert.errors.count, "error")} prohibited this buyer from being saved:"
        %ul
          - @designer_electric_cert.errors.full_messages.each do |msg|
            %li= msg
    = render 'layouts/sup_desg_one_form', f: f
